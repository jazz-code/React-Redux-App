
const initialState = {
    weather: [],
    test: "test"
}

export const weatherReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}